name: Update Notion Movies from TMDB

on:
  schedule:
    - cron: '*/15 * * * *'  # Every 15 minutes
  workflow_dispatch:        # You can also run it manually from GitHub

jobs:
  run-script:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install -r requirements.txt
      - name: Run TMDB Notion Updater
        run: python tmdb_updater.py


        https://www.notion.so/robleto/1e2a0765760b8051ae83f6e2aa3b4931?v=1e2a0765760b81cf9319000cfd6ba1c7&pvs=4